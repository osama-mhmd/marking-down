type Config = {
  styles?: {
    heading?: string;
    hyperlink?: string;
    ul?: string;
    embed?: string;
    embedHeader?: string;
    embedBody?: string;
    embedFooter?: string;
  };
};

export default Config;
