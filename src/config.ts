import Config from "./config.d";

const defaultConfig: Config = {
  styles: {
    heading: "",
    li: "",
    hyperlink: "",
    card: "card",
    paragraph: "muted",
  },
};

export default defaultConfig;
